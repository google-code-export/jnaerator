svn update

if [[ "$FORCE" == "" ]] ; then
		if [ ! `svn status | wc -l` -eq 0 ] ; then
		echo There are SVN modifications here : 
		svn status
		exit 1 ;
	fi ;
fi

find sources/com -exec cp '/Users/ochafik/Prog/Java/{}' '{}' ';'
svn status
